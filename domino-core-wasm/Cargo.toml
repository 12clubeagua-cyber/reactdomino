[package]
name = "domino-core-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
# Optimize for size and performance (The Akita Way)
lto = true
opt-level = "z"
panic = "abort"
